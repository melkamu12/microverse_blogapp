<div class="container-page">
<h1>Posts #new Page</h1>

<%= form_with(model: [current_user, Post.new]) do |form| %>
    <%= form.label :title %>
    <%= form.text_field :title %>
  
    <%= form.label :text %>
    <%= form.text_area :text %>
  
    <%= form.submit 'Create Post' %>
  <% end %>
</div>